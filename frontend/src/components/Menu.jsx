import React from 'react'
import {data} from '../restApi.json'
const Menu = () => {
  return (
    <>
      <section className='menu' id='menu'>
        <div className="container">
            <div className="heading_section">
                <h1 className="heading">POPULAR DISHES</h1>
                <p> Indulge in our curated selection of delectable dishes, meticulously prepared with fresh, local ingredients. 
              Our menu offers something for every palate, from savory delights to sweet treats. 
              Whether you're seeking a hearty meal or a light bite, we invite you to explore our culinary offerings and discover your new favorite dish.</p>
            </div>
            <div className="dishes_container">
                {
                    data[0].dishes.map(element => (
                        <div className="card" key={element.id}>
                                <img src={element.image} alt={element.title} />
                                <h3>{element.title}</h3>
                                <button>{element.category}</button>
                        </div>
                    ))
                }   
            </div>
        </div>
      </section>
    </>
  )
}

export default Menu
